# [[[cog
# import os, sys
# try:
#     from karabogui import _version
# except ImportError:
#     print('\nPlease run setup install so the version is generated')
# else:
#     from karabogui import globals
#     cog.outl(f'{{% set version = "{globals.GUI_VERSION}" %}}')
# ]]]
# [[[end]]]
{% set project_root = os.environ['CI_PROJECT_DIR'] %}

# set the project root directory by setting the CI_PROJECT_DIR environment var
# on conda build

package:
  name: karabogui
  version: {{ version }}

source:
  path: {{ project_root }}
  git_rev: {{ version }}

build:
  number: 0
  entry_points:
    - karabo-gui=karabogui.programs.gui_runner:main
    - panel-runner=karabogui.programs.panel_runner:main
    - karabo-cinema=karabogui.programs.cinema:main
    - karabo-theatre=karabogui.programs.theatre:main
    - karabo-update-extensions=karabogui.dialogs.update_dialog:main

requirements:
  build:
    - python=3.6.7
    - jupyter_client=5.3.3
    - setuptools=42.0.1
    - setuptools_scm=3.3.3
  run:
    # [[[cog
    # import cog
    # import json
    # import os
    # import subprocess
    # conda = os.environ.get('CONDA_EXE', 'conda')
    # output = subprocess.check_output([conda, 'env', 'export', '-n', 'karabogui', '--json'])
    # for dep in json.loads(output)['dependencies']:
    #     cog.outl(f'- {dep}')
    # ]]]
    # [[[end]]]

test:
  imports:
    - karabogui
    - karabo.native
    - karabo.common
