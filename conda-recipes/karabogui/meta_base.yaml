# This file is part of Karabo.
#
# http://www.karabo.eu
#
# Copyright (C) European XFEL GmbH Schenefeld. All rights reserved.
#
# Karabo is free software: you can redistribute it and/or modify it under
# the terms of the MPL-2 Mozilla Public License.
#
# You should have received a copy of the MPL-2 Public License along with
# Karabo. If not, see <https://www.mozilla.org/en-US/MPL/2.0/>.
#
# Karabo is distributed in the hope that it will be useful, but WITHOUT ANY
# WARRANTY; without even the implied warranty of MERCHANTABILITY or
# FITNESS FOR A PARTICULAR PURPOSE.
# [[[cog
# from setuptools_scm import get_version
# from os import path as op
# root = op.join(op.dirname(cog.inFile), op.pardir, op.pardir)
# root = op.abspath(root)
# version = get_version(root=root)
# project_root = root.replace('\\', '\\\\')
# cog.outl(f'{{% set version = "{version}" %}}')
# cog.outl(f'{{% set project_root = "{project_root}" %}}')
# ]]]
# [[[end]]]

package:
  name: karabogui
  version: {{ version }}

source:
  path: {{ project_root }}
  git_rev: {{ version }}

build:
  number: 0
  entry_points:
    - karabo-gui=karabogui.programs.gui_runner:main
    - panel-runner=karabogui.programs.panel_runner:main
    - karabo-cinema=karabogui.programs.cinema:main
    - karabo-concert=karabogui.programs.concert:main
    - karabo-theatre=karabogui.programs.theatre:main
    - karabo-update-extensions=karabogui.dialogs.update_dialog:main
  # This is needed to avoid removing egg files during packaging with setuptools
  preserve_egg_dir: True
  pin_depends: strict

requirements:
  # [[[cog
  # import cog
  # from pathlib import Path
  # from collections import defaultdict
  #
  # current_file = Path(cog.inFile)
  # file_path = current_file.parent.resolve() / 'environment.devenv.yml'
  # group_prefix = '# +++'
  # valid_groups = ('build', 'run')
  # to_write = defaultdict(str)
  # capture = False
  #
  # with open(file_path, 'r') as f:
  #     for line in f:
  #         stripped = line.strip()
  #         if stripped.startswith('dependencies:'):
  #             capture = True
  #             continue
  #         if not capture:
  #             continue
  #         if not stripped.startswith('#') and stripped.endswith(':'):
  #             break  # next top-level section, stop
  #         if stripped.startswith(group_prefix):
  #             key = stripped[len(group_prefix):].strip()
  #             if not key in valid_groups:
  #                 continue
  #         if stripped.startswith('-'):
  #             to_write[key] += f'    {stripped}\n'
  #
  # for group in valid_groups:
  #     if to_write[group]:
  #         cog.outl(f'  {group}:')
  #         cog.outl(f'{to_write[group]}')
  # ]]]
  # [[[end]]]

test:
  imports:
    - karabogui
    - karabo.native
    - karabo.common
