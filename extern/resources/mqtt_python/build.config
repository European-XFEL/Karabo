CUSTOM_BUILD=y
CWD=$(pwd)

MQTTPY=paho.mqtt.python-1.5.1

echo
echo "### Trying to install $MQTTPY"
echo

rm -rf $MQTTPY

if [ ! -d "$MQTTPY" ]; then
        tar -xf ${MQTTPY}.tar
fi
cd $MQTTPY
$INSTALL_PREFIX/bin/python setup.py install

cd $CWD
