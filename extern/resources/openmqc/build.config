DEP_NAME="openmqc-5.1.4.1"
DEP_EXT="tar"
EXTRACT_COMMAND="tar -xf $DEP_NAME.$DEP_EXT"
CONFIGURE_COMMAND=""
MAKE_COMMAND="make -j$NUM_CORES EXTERN_DISTDIR=$INSTALL_PREFIX"
INSTALL_COMMAND="patchelf --force-rpath --set-rpath '\$ORIGIN/../lib' lib/*.so; cp -rf include/openmqc $INSTALL_PREFIX/include/; cp -rf lib $INSTALL_PREFIX"
