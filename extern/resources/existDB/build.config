#we have an uncompressed jar file
DEP_NAME=""
DEP_EXT=""
EXTRACT_COMMAND=""
CONFIGURE_COMMAND=""
MAKE_COMMAND=""
#we copy the file so it is available when we need to install it
INSTALL_COMMAND="mkdir $INSTALL_PREFIX/eXistDB; cp $PWD/* $INSTALL_PREFIX/eXistDB/"
