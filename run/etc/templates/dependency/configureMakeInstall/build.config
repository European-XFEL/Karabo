#
# Build configuration file for __CLASS_NAME__
#
# Author: <__EMAIL__>
# Date:    __DATE__
#

# This script will be sourced in bash context.
# This means you can use bash functionality to your liking.
#
# Following variables are available from outside:
#
# INSTALL_PREFIX  The location where this dependency should be installed to
# NUM_CORES       The number of cores availabe for a parallel build
# PYTHON          The python interpreter
# KARABO          The location of the karaboFramework base directory
# 

# Please define the following variables for an automated build (just leaf non-used variables empty)
# NOTE: Always use double-quotes for the variable's values

# The file base name (without extension)
DEP_NAME="__CLASS_NAME__"

# The file extension (e.g. "tar.gz", or "zip", etc)
DEP_EXT="tar.gz"

# The command to extract the archive (e.g. "tar -xzf $DEP_NAME.$DEP_EXT")
EXTRACT_COMMAND="tar -xzf $DEP_NAME.$DEP_EXT"

# The configure command (e.g. "./configure --prefix=$INSTALL_PREFIX")
CONFIGURE_COMMAND="./configure --prefix=$INSTALL_PREFIX --libdir=$INSTALL_PREFIX/lib"

# The make command (e.g. "make -j$NUM_CORES")
MAKE_COMMAND="make -j$NUM_CORES"

# The install command (e.g. make install)
INSTALL_COMMAND="make install"
